# E029｜RGBNT201 T12共享跨模态token重建

- 状态：运行中
- 开始时间：2026-08-27 01:20 CST
- 对应论文实验：T12
- 实验目的：检验TOP-ReID CRM式token重建监督能否在不增加独立ViT主干的前提下，补足共享ViT K1路径的跨模态表征容量。
- 唯一变化：相对E015/K1，仅增加训练期共享、目标模态条件化的轻量token重建头。每个batch随机选一个目标模态，预测器只读取其他两模态；目标tokens stop-gradient。K1选择器、FACR、识别损失和推理描述符路径不变。
- 配置：`configs/RGBNT201/paper/base.yml` + `configs/RGBNT201/fusion/t12_sfts_k1_shared_token_recon.yml`
- 数据集与协议：RGBNT201 `train_171`训练、`test`评估；三模态联合评估；每个epoch验证；关闭re-ranking。
- Seed / batch / epoch：1111 / 40 / 20
- 输入尺寸：384×192；每身份实例数8
- Optimizer / LR / weight decay：AdamW / 1e-4 / 0.05；backbone LR factor 0.1
- 重建设置：hidden dim 256；归一化余弦损失系数0.1；前5 epoch warmup；每批重建一个目标模态。
- 预训练权重：`/root/autodl-tmp/pretrained/vit_base_patch16_224_augreg2_in21k_ft_in1k.pth`
- 计划命令：`/root/miniconda3/bin/python tools/run_rgbnt201_fusion.py --single-experiment E029 --single-row T12 --single-config configs/RGBNT201/fusion/t12_sfts_k1_shared_token_recon.yml --single-output-name E029_T12_sfts_k1_shared_token_recon_seed1111 --seed 1111`；runner内部使用`timeout --signal=TERM --kill-after=10s 30m`。
- 输出目录：`/root/autodl-tmp/outputs/HTL-ReID/E029_T12_sfts_k1_shared_token_recon_seed1111`
- Git commit：`0daf4d2`
- Runner PID：1731（launcher shell PID 1730）
- Runner日志：`/root/autodl-tmp/outputs/HTL-ReID/E029_T12.runner.log`
- 预期产物：resolved_config.yml、commit.txt、command.txt、stdout.log、train_log.txt、run_result.json、DONE/FAILED标记、最佳checkpoint及retention.json。
- 预设判断：相对同seed E015/K1的63.31 mAP、66.99 Rank-1，mAP至少提高1.0个百分点且Rank-1不下降，才进入seed 2222验证；否则停止T12追加实验并保留K1。
- 启动检查：远端仓库已同步到`0daf4d2`，训练进程由GNU timeout保护，epoch 1已正常进入迭代；GPU利用率99%，显存约24.3 GiB。
- 结果：等待训练完成后填写。
- 结论：等待训练完成后填写。
