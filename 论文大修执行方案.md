# HTL-ReID 论文大修执行方案

## 一、当前修改方向（K1已锁定）

旧论文的“HS + FACSS + AGF”三阶段主线已经放弃。控制实验否定了T10丢弃信息摘要与受限自细化。三seed配对结果进一步锁定K1为最终结构：

1. 共享权重ViT-B/16；
2. T2让每个模态CLS同时读取另外两个模态的完整patch，并通过三轮样本自适应双源路由完成融合；
3. K1在T2路由前加入直接参考Magic Tokens/EDITOR的固定K=1 SFTS共享硬mask，不加入T10残差摘要和最终自细化；
4. 三个更新后的CLS直接接受CE + Triplet监督。

三seed统计为：T2 63.847±0.550 mAP、65.390±0.661 Rank-1；K1 64.037±0.823、67.187±0.561。K1相对T2的逐seed mAP差为-0.51/+0.52/+0.56，平均+0.190；Rank-1差为+1.92/+1.67/+1.80，平均+1.797且方向一致。按预先规则锁定K1，不再追加seed或修改主结构。TPM不能通过小改名包装成原创；论文必须明确引用TOP-ReID，K1必须明确引用Magic Tokens/EDITOR。

T11稳定性验证已失败，不替换K1。当前另外实现了待验证T12：仅在训练期用共享、模态条件化的轻量重建头从两个源模态预测stop-gradient目标patch tokens。T12不改变K1/FACR推理路径，尚未进行正式训练，因此当前论文主方法仍为K1。

### 设计来源与引用边界

本轮方案来自`ReID相关论文`本地论文集的针对性检索，论文与实现的对应关系固定如下：

1. Magic Tokens/EDITOR是K1所用SFTS硬选择的直接来源；K1采用其空间/频率选择与共享mask规则，但以FACR替代HMA，并且不采用BCC、OCFR；
2. TOP-ReID是TPM交叉模态token读取与循环聚合的直接来源；FACR在此问题设定上改为双来源同时读取和样本自适应路由，必须通过对照实验及文字明确区别于原TPM。T12的token重建监督直接受TOP-ReID CRM启发，必须引用CRM；只能把共享轻量预测器、模态条件化和训练期设计写为针对共享ViT的实现差异，不得声称重建思想原创；
3. DeMo/ATMoE可用于说明动态、上下文相关的模态加权研究背景，但当前没有FACR直接复现ATMoE结构或代码的证据，只能列为相关工作和概念对照；
4. T10的丢弃patch残差摘要和受限自模态细化属于针对SFTS硬筛选信息损失与E020失败结果提出的项目扩展，但E024未复现优势，因此只保留为失败消融，不能写成论文贡献。

继续删除或停用：旧AGF包装、Modality Adapter、Part Branch、QAWF、质量感知频域分支、T10残差摘要/自细化及其配套辅助损失。由于T3评分引导已否定，BCC和OCFR不再叠加到K1主线；T12是唯一待决策的新辅助损失，必须单独受预设门槛约束。

论文不声称突出计算效率或全面缺失模态鲁棒性。E007–E026已完成结构筛选与三seed决策，K1方法主线已经锁定；下一阶段进入跨数据集、计算开销和论文正文更新。

## 二、需要立即纠正的代码—论文不一致

当前论文的实现细节已经与代码不符，所有旧描述和旧结果必须在复现实验后更新。

| 项目 | 当前论文 | 当前代码 |
|---|---|---|
| 主干预训练 | ImageNet-1K ViT-B/16 | AugReg2，ImageNet-21K预训练后在ImageNet-1K微调 |
| RGBNT201输入 | 256×128 | 384×192 |
| 车辆数据输入 | 128×256 | 192×384 |
| 优化器 | SGD | AdamW |
| 基础学习率 | 0.001 | 0.0001 |
| Weight decay | 1e-4 | 0.05 |
| Batch size | 64 | RGBNT201/RGBNT100默认128，A2覆盖配置为40；MSVR310为40 |
| Token数量 | 固定 k=16 | HS使用16；FACSS动态K为8–32 |
| 主方法 | HS + FACSS + AGF | 固定K=1 SFTS共享mask + 三轮样本自适应FACR；T10已否定 |
| 测试 | 未充分说明 | 论文主结果明确关闭re-ranking |

必须建立一个独立、明确的论文配置，避免依赖默认配置中仍然开启的AGF、Adapter和Part Branch。

## 三、第一阶段：冻结论文配置

在运行正式实验前完成以下工作：

1. 建立唯一的 paper 配置文件；
2. 明确设置 `AGF=0`、`MODALITY_ADAPTER=0`、`PART_BRANCH=0`；
3. 关闭所有非论文辅助损失；
4. 明确使用 `vit_base_patch16_224_augreg2_in21k_ft_in1k.pth`；
5. 主结果关闭 re-ranking；如需要，可把 re-ranking 结果放入补充表；
6. 频域 token 分支先从论文路径关闭，避免引入一个新的核心模块和额外消融；
7. 固定输入尺寸、优化器、训练周期、batch size、随机种子和测试协议；
8. 所有baseline与完整模型必须使用完全相同的主干和预训练权重。

如果关闭频域分支导致性能出现明显下降，再增加一次“频域开启/关闭”对照，决定是否将其纳入论文；不得在论文中隐瞒实际启用的模块。

## 四、第二阶段：RGBNT201决策实验

先只在RGBNT201上执行以下四个训练任务：

| 编号 | 配置 | 要回答的问题 |
|---|---|---|
| M0 | 强共享ViT-B/16 baseline | 当前性能有多少来自强基线 |
| M1 | M0 + HS | 多层空间候选是否有效 |
| M2 | M1 + FACSS | FACSS是否具有独立贡献 |
| M3 | M2 + QAWF | 质量感知权重是否值得保留 |

所有配置使用相同的：

- AugReg2 ImageNet-21K→ImageNet-1K权重；
- 输入分辨率；
- 数据增强；
- 优化器和学习率计划；
- batch采样方式；
- 训练周期；
- 随机种子；
- 测试协议。

### 决策规则

1. 如果M2明显优于M1，FACSS可以继续作为核心贡献；
2. 如果M2与M1基本持平，停止后续大规模实验，不继续修改模型追逐结果，重新考虑论文定位；
3. 如果M3没有稳定提升，或者质量分数不能随模态退化合理变化，删除QAWF，只保留HS+FACSS；
4. 不要求所有模块结果绝对单调；没有稳定证据的模块从论文中删除；
5. 只有RGBNT201决策实验通过后，才运行RGBNT100和MSVR310。

当前决策结果：E004相对E003下降3.47个百分点mAP和5.26个百分点Rank-1，QAWF已按规则删除。E003相对E002提升0.77个百分点mAP和1.19个百分点Rank-1，FACSS暂时保留，但须先完成同token预算和匹配策略针对性验证。

### 融合扩展决策（E006–E008）

| 编号 | 配置 | 要回答的问题 | 当前状态 |
|---|---|---|---|
| T1 / E006 | M0 + 纯TPM | 正确循环TPM在共享ViT下是否有效 | 已完成：61.36 mAP、61.72 Rank-1，低于M0 |
| T2 / E007 | M0 + 自适应全连接路由 | 动态路由能否优于固定TPM | 已完成：63.82 mAP、65.07 Rank-1，全tokens主线 |
| T3 / E008 | HS/FACSS连续评分 + FACR | FACSS软引导能否优于T2和M2 | 已完成：62.70/61.00，不采用 |
| T7 / E014–E018 | 固定K SFTS + FACR | 稀疏选择的K敏感性 | 已完成：K1最高Rank-1，K16最高固定K mAP |
| T8 / E019 | K16 + 路由均衡损失 | 原FACR是否存在来源塌缩 | 已完成：退化，不采用 |
| T9 / E020 | T2 + 全量自身patch细化 | 自身信息重读能否补足FACR | 已完成：62.33/61.12，不采用 |
| T10 / E021、E024 | K1 + 丢弃信息摘要 + masked FACR自细化 | 只补回被筛掉的信息是否有效 | seed 2222未复现优势，不采用 |
| T11 / E027、E028 | K1 + FACR前置独立masked aggregation | HMA式先独立聚合能否稳定改善K1 | seed 2222未复现主指标优势，不采用 |
| T12 / E029 | K1 + 训练期共享token重建 | CRM式特征监督能否补足共享ViT容量 | 已授权GPU正式训练，待启动 |

TPM循环为：RGB CLS依次读取NIR、TIR、RGB patches；NIR依次读取TIR、RGB、NIR；TIR依次读取RGB、NIR、TIR。当前项目共享ViT，TOP-ReID使用三个独立ViT，因此E006只评价TPM模块在当前框架中的作用。

当前决策：FACSS评分引导、路由均衡损失、全量自身patch细化、T10丢弃信息摘要自细化与T11前置独立聚合均已否定。K1是当前最终方法；T12仅是代码完成的待验证训练期监督，在通过预注册门槛前不进入论文主方法。

## 五、第三阶段：T2/K1最终选择（已完成）

三seed配对已经完成，最终选择固定K=1 SFTS + FACR：

1. K1三seed平均mAP较T2高0.190，Rank-1平均高1.797且三个seed方向一致；
2. seed 1111/2222/3333均保持相同主干、预训练、输入、batch、采样器、优化器、20 epoch和关闭re-ranking协议；
3. E007/E015/E022/E023/E025/E026均保留最佳checkpoint、resolved config、日志和结果JSON；
4. K1三seed平均保留率约11.561%，但mask只限制注意力可见性，并未物理删除FACR输入张量，因此不能换算为FLOPs或速度提升；
5. 不再运行T2/K1额外seed、T10/T11、门控权重搜索或未预注册的新辅助损失。T12若获授权，只先运行seed 1111并以“相对E015至少+1.0 mAP且Rank-1不下降”为门槛；否则直接进入跨数据集与论文更新。

## 六、第四阶段：主结果与稳定性

RGBNT201决策已经通过，当前执行状态：

1. RGBNT201 K1的seed 1111/2222/3333已经完成，报告三次运行mean±std；
2. RGBNT100最终K1模型一次，待运行；
3. MSVR310最终K1模型一次，待运行；
4. 跨数据集配置继续关闭re-ranking及所有已否定模块。
5. T12只完成代码，未进入当前有效结果或主方法。

### 常规训练次数

| 任务 | 次数 | 状态 |
|---|---:|---|
| RGBNT201组件配置M0–M3 | 4 | 已完成 |
| RGBNT201 K1额外两个种子 | 2 | 已完成 |
| RGBNT201 T12单seed门槛验证 | 最多1 | 待决定；未启动 |
| RGBNT100最终K1模型 | 1 | 待运行 |
| MSVR310最终K1模型 | 1 | 待运行 |

不再追加RGBNT201无门槛结构搜索或额外seed；除待决定的T12单seed门槛验证外，后续训练仅限已锁定K1的跨数据集验证。

## 七、第五阶段：两个低成本评估

以下评估不需要重新训练。

### 1. 质量与模态退化分析

只在RGBNT201测试：

- 完整三模态；
- 分别缺失RGB、NIR、TIR；
- 或对一个模态施加轻度、中度、重度退化；
- 记录质量分数变化和mAP变化。

删除原稿中的六种缺失组合以及第二个数据集上的缺失模态实验。新标题不再强调“Robust”，无需证明所有极端情况。

### 2. 计算开销

即使不再声称效率高，仍需报告：

- 参数量；
- GFLOPs；
- RTX 4090、batch=1下的FPS或单图延迟；
- 峰值显存。

只比较M0与最终模型，统一输入尺寸和硬件环境。该表称为“computational footprint”，不声称高效，也不需要重新训练。

## 八、第六阶段：论文修改顺序

必须先锁定实验结果，再按以下顺序修改正文，避免重复返工。

### 1. Methods

- 删除AGF章节、公式、图示和相关贡献声明；
- 新增FACR与K1 SFTS共享mask章节并明确引用Magic Tokens/EDITOR。删除丢弃信息摘要和最终自模态细化的主方法描述，只在消融中报告T10失败结果；明确FACR与TOP-ReID固定TPM循环的区别；
- 删除“joint optimization”，改为“architectural dependency”或“architectural coupling”；
- 将HS定义为空间候选先验，不再称为多粒度特征提取；将修改后的FACSS定义为连续token重要性评分器；
- FACSS评分引导和T10自细化已否定，不进入最终主路径；最终核心贡献收窄为K1共享选择条件下的样本自适应跨模态路由，并严格区分引用模块与项目扩展；
- 不声称FACSS评分理论最优，只表述为经实验验证的设计；
- 删除QAWF方法描述、公式和贡献声明；
- 解释固定层4、8、12的经验选择，并把层选择与token预算作为局限性。

### 2. Implementation Details

完整披露：

- ViT-B/16 AugReg2，ImageNet-21K预训练后在ImageNet-1K微调；
- 各数据集真实输入尺寸；
- AdamW、基础学习率、weight decay、backbone学习率系数；
- 实际训练周期、cosine schedule和warm-up；
- 各数据集batch size和每个身份实例数；
- label smoothing与soft-margin triplet loss；
- 随机种子；
- HS层位置、HS token数、FACSS动态K范围；
- SIE使用情况；
- 主结果是否关闭re-ranking。

### 3. 实验结果

- 删除所有无法由当前代码和日志复现的旧数字；
- 主表标注Backbone、Pre-training、Resolution、Re-ranking；
- CLIP方法作为强预训练参考，不与本方法作严格公平性结论；
- 重点报告相同预训练条件下M0与最终模型的差异；
- 报告三次运行的mean±std；
- 使用收敛后的真实结果更新摘要、引言和结论。

### 4. Related Work与方法定位

补充：

- EDITOR、TOP-ReID、DeMo、MambaPro；
- DynamicViT、ToMe、AdaToken、EViT；
- 审稿人指定的遮挡、空间频率、多粒度、关系图方法；
- 质量感知和不确定性融合相关工作。

新颖性仅聚焦以下区别：

> 现有方法通常先独立选择token，再进行融合；FACSS让token保留标准显式考虑其跨模态融合价值。

避免声称HS、门控或注意力本身是全新结构。

### 5. 标题、摘要、引言、讨论和结论

这些部分最后修改，确保与最终结果一致：

- 扩充摘要，使其包含问题、真实主干、核心方法、实验设置和主要证据；
- 删除SOTA、显著优越、理论最优等过强表述；
- 删除AGF贡献和无法支撑的全面鲁棒性声明；
- 讨论部分增加局限性；
- 结论只总结实验真正支持的内容。

### 6. 图、公式与格式

- 重画Figure 2，只展示最终实验确认的真实流程；
- Figure 2显示K=1 SFTS共享硬mask、完整长度token张量在mask约束下进入FACR（并非物理token压缩），以及每个CLS同时读取另外两个模态的可见patch；不得画入T10残差摘要、自细化或FACSS评分路径；
- 标明token数量和张量维度；
- 在图中展示SFTS共享mask的形成、FACR双源路由权重与门控，以及mask约束下的可见范围；被丢弃patch残差摘要、FACSS的self score、cross score与α仅属于已否定的历史消融，不进入最终方法主图；
- 改进Figures 1和2的图注；
- 统一 `F_m^{hie}` 与 `P_m^{hier}` 等符号；
- 检查所有公式、表号、图号和正文引用；
- 在Declarations下分别设置Ethical approval、Consent to participate、Consent to publish；
- 全文英语校对。

## 九、第七阶段：逐条回复审稿人

回复信应在论文定稿后编写，并逐条标出修改位置、表号和图号。

AGF相关意见统一采取接受并删除的处理方式：

> 根据审稿人的建议，我们进行了更严格的同主干控制实验。由于更新后的实验未能证明AGF具有稳定且独立的贡献，我们在修订稿中删除了AGF模块、相关贡献声明和鲁棒性过度表述，并将论文聚焦于有充分证据支持的FACSS机制。

其余主要回复策略：

- “联合优化”问题：承认并改为架构顺序依赖；
- 理论依据不足：不再声称最优，增加同token预算和匹配策略对照；
- 预训练公平性：披露完整AugReg2设置并增加同主干baseline；
- 统计显著性：报告RGBNT201三次运行均值和标准差；
- 效率质疑：提供baseline与最终模型的参数、GFLOPs、FPS和显存；
- 鲁棒性质疑：收窄声明，只提供单模态退化/缺失的受控分析；
- 相关工作不足：补充审稿人点名文献；
- 图示和符号问题：重画Figure 2并统一数学符号。

## 十、执行原则

1. 先跑RGBNT201四个决策配置，再修改论文主体；
2. 不保留没有稳定证据的模块；
3. 不因某个结果不好而反复修改模型，避免形成无休止的训练循环；
4. 不把强预训练带来的提升归因于FACSS；
5. 所有表格数字必须能追溯到checkpoint、配置、随机种子和日志；
6. 外部SOTA结果仅作背景比较，核心证据来自同主干、同预训练的内部控制实验；
7. 最终提交包括干净修订稿、逐条回复PDF、必要时的高亮修改稿，以及与论文一致的代码配置。
8. 单次训练墙钟时间不得超过30分钟；M0–M3统一训练20 epoch并保留120-epoch调度合同，所有启动命令使用硬超时限制。
